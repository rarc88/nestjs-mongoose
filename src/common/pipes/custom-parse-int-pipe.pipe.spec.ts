import { CustomParseIntPipePipe } from './custom-parse-int-pipe.pipe';

describe('CustomParseIntPipePipe', () => {
  it('should be defined', () => {
    expect(new CustomParseIntPipePipe()).toBeDefined();
  });
});
